import { Task } from "@prisma/client";
import db from "../../prisma/database";

// export async function FindOneTask(id: number): Promise<Task | null> {
//   return db.task.findUnique({ where: { id: id } });
// }
